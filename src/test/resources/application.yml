spring:
  datasource:
    datasource:
    url: jdbc:postgresql://localhost:5432/journey_test
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
      properties:
        hibernate:
          show_sql: true
          use_sql_comments: true
          format_sql: true

log4j:
  logger:
    org.hibernate:
      type: trace 

